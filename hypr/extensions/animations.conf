animations {
    enabled = yes

    bezier = easeOutQuint, 0.23, 1, 0.32, 1
    bezier = swiftOut, 0.55, 0, 0.1, 1

    animation = windows, 1, 3, easeOutQuint
    animation = windowsIn, 1, 3, easeOutQuint, popin 90%
    animation = windowsOut, 1, 2, swiftOut, popin 90%
    animation = border, 1, 3, default
    animation = fade, 1, 3, default
    animation = workspaces, 1, 3, easeOutQuint, slide
    animation = layers, 1, 3, easeOutQuint, fade
}
